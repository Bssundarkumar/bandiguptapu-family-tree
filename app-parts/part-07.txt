.14);if(hits.length===1)selectPerson(hits[0],true)}});search.addEventListener('keydown',e=>{if(e.key==='Enter'){const q=search.value.trim().toLowerCase(),p=people.find(p=>JSON.stringify(p).toLowerCase().includes(q));if(p)selectPerson(p.id,true)}})
// Drawer + CRUD
const back=document.getElementById('drawerBack'),form=document.getElementById('personForm');function openDrawer(){back.classList.add('show');document.body.style.overflow='hidden'}function closeDrawer(){back.classList.remove('show');document.body.style.overflow=''}function clearForm(){form.reset();document.getElementById('personId').value='';document.getElementById('relationType').value='';document.getElementById('relationTarget').value='';document.getElementById('relationHint').textContent='';document.getElementById('deleteBtn').style.display='none'}
function openAdd(){clearForm();document.getElementById('drawerTitle').textContent='Add person';openDrawer()}function openEdit(id){const p=byId(id);if(!p)return;clearForm();document.getElementById('drawerTitle').textContent='Edit person';fields.forEach(k=>document.getElementById(k).value=p[k]??'');document.getElementById('personId').value=id;document.getElementById('deleteBtn').style.display='block';openDrawer()}
function openRelation(type){const t=byId(current);clearForm();document.getElementById('drawerTitle').textContent='Add '+type;document.getElementById('relationType').value=type;document.getElementById('relationTarget').value=t.id;document.getElementById('relationHint').textContent=`This person will be linked as ${type} of ${fullName(t)}.`;if(['child','parent'].includes(type))document.getElementById('surname').value=t.surname;openDrawer()}
function link(a,id){if(!a.includes(id))a.push(id)}function unlink(a,id){const i=a.indexOf(id);if(i>=0)a.splice(i,1)}function applyRel(n,type,target){const t=byId(target);if(!t)return;if(type==='child'){link(t.children,n.id);link(n.parents,t.id)}if(type==='parent'){link(t.parents,n.id);link(n.children,t.id)}if(type==='spouse'){link(t.spouses,n.id);link(n.spouses,t.id)}}
form.onsubmit=e=>{e.preventDefault();const id=document.getElementById('personId').value;let p=id?byId(id):normalize({});fields.forEach(k=>p[k]=document.getElementById(k).value.trim());if(!id){people.push(p);const r=document.getElementById('relationType').value,t=document.getElementById('relationTarget').value;if(r&&t)applyRel(p,r,t);current=p.id}save();closeDrawer();toast('Family tree updated')};document.getElementById('deleteBtn').onclick=()=>{const id=document.getElementById('personId').value;if(!id||!confirm('Delete this person and remove all relationship links?'))return;people.forEach(p=>{unlink(p.parents,id);unlink(p.spouses,id);unlink(p.children,id)});people=people.filter(p=>p.id!==id);current=people.find(p=>p.id==='satya-sundar-kumar')?.id||people[0]?.id;save();closeDrawer();toast('Person removed')}
// Buttons
function openProfileDrawer(){renderSide();const drawer=document.querySelector('.drawer');drawer.querySelectorAll('.mobileProfileClone').forEach(x=>x.remove());const clone=document.getElementById('side').cloneNode(true);clone.className='mobileProfileClone';clone.style.cssText='display:block;border:0;padding:0;margin-top:4px;background:transparent;overflow:visible';clearForm();document.getElementById('drawerTitle').textContent='Person details';form.style.display='none';drawer.appendChild(clone);openDrawer();const cleanup=()=>{form.style.display='block';clone.remove();document.getElementById('closeDrawer').removeEventListener('click',cleanup);document.getElementById('cancelBtn').removeEventListener('click',cleanup)};document.getElementById('closeDrawer').addEventListener('click',cleanup);document.getElementById('cancelBtn').addEventListener('click',cleanup)}
document.getElementById('addBtn').onclick=openAdd;document.getElementById('closeDrawer').onclick=closeDrawer;document.getElementById('cancelBtn').onclick=closeDrawer;back.onclick=e=>{if(e.target===back)closeDrawer()};document.getElementById('fitBtn').onclick=()=>{if(cy)cy.animate({fit:{eles:cy.elements(),padding:70}},{duration:550});else document.getElementById('fallback').scrollTo({top:0,behavior:'smooth'})};document.getElementById('homeBtn').onclick=()=>selectPerson('satya-sundar-kumar',true);document.getElementById('mobileProfile').onclick=openProfileDrawer;
const navTree=document.getElementById('navTree'),navHome=document.getElementById('navHome'),navProfile=document.getElementById('navProfile'),navAdd=document.getElementById('navAdd');
function navActive(btn){document.querySelectorAll('#mobileNav button').forEach(b=>b.classList.remove('active'));btn?.classList.add('active')}
navTree.onclick=()=>{navActive(navTree);if(cy)cy.animate({fit:{eles:cy.elements(),padding:55}},{duration:420});else document.getElementById('fallback').scrollTo({top:0,behavior:'smooth'})};
navHome.onclick=()=>{navActive(navHome);selectPerson('satya-sundar-kumar',true)};
navProfile.onclick=()=>{navActive(navProfile);openProfileDrawer()};
navAdd.onclick=()=>{navActive(navAdd);openRelation('child')};
// Backup + import by long-press-ish click menu
function exportData(){const blob=new Blob([JSON.stringify({version:3,exportedAt:new Date().toISOString(),people,pdfArchive:PDF_ARCHIVE},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='bandiguptapu-family-backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}document.getElementById('exportBtn').onclick=exportData;

function renderDirectory(q=''){const grid=document.getElementById('directoryGrid');const term=q.trim().toLowerCase();const arr=people.filter(p=>!term||JSON.stringify(p).toLowerCase().includes(term)).sort((a,b)=>fullName(a).localeCompare(fullName(b)));document.getElementById('directoryCount').textContent=arr.length;grid.innerHTML=arr.map(p=>`<button class="directoryCard" data-id="${p.id}"><b>${fullName(p)}</b><small>${[p.occupation,p.ancestralVillage,p.branchGroup,'Generation '+(generation(p)+1)].filter(Boolean).join(' · ')}</small></button>`).join('');grid.querySelectorAll('.directoryCard').forEach(b=>b.onclick=()=>{document.getElementById('directorySheet').classList.remove('show');selectPerson(b.dataset.id,true)})}
document.getElementById('allPeopleBtn').onclick=()=>{renderDirectory();document.getElementById('directorySheet').classList.add('show')};document.getElementById('closeDirectory').onclick=()=>document.getElementById('directorySheet').classList.remove('show');document.getElementById('directorySearch').oninput=e=>renderDirectory(e.target.value);
function panMap(dir){const fb=document.getElementById('fallback'),dx=Math.max(260,fb.clientWidth*.65),dy=Math.max(260,fb.clientHeight*.55);const map={left:[-dx,0],right:[dx,0],up:[0,-dy],down:[0,dy]};const [x,y]=map[dir]||[0,0];fb.scrollBy({left:x,top:y,behavior:'smooth'})}
document.querySelectorAll('[data-pan]').forEach(b=>b.onclick=()=>panMap(b.dataset.pan));document.getElementById('centerMap').onclick=()=>{const active=document.querySelector('.fcard.active');if(active)active.scrollIntoView({behavior:'smooth',block:'center',inline:'center'})};
render();
</script>
</body>
</html>