id,source:p.id,target:cid,type:'parent'}})});p.spouses.forEach(sid=>{if(p.id<sid&&byId(sid))edges.push({data:{id:'sp-'+p.id+'-'+sid,source:p.id,target:sid,type:'spouse'}})})});return edges}
function nodeData(){return people.map(p=>({data:{id:p.id,label:fullName(p),sub:[p.nickname&&'“'+p.nickname+'”',p.occupation,p.ancestralVillage].filter(Boolean).join(' • '),gender:p.gender,confidence:p.confidence,gen:generation(p)}}))}
function renderFallbackGraph(){
  const fb=document.getElementById('fallback'); fb.classList.add('show');
  const g=groups();
  fb.innerHTML=`<div class="box"><div class="fallbackTree">${g.map((arr,i)=>`<section class="fgen" data-g="${i}"><div class="fgenHead">Generation ${i+1}<br><span>${arr.length} people</span></div><div class="frow">${arr.map(p=>`<button class="fcard ${p.id===current?'active':''}" data-id="${p.id}"><strong>${fullName(p)}</strong><small>${[p.nickname&&'“'+p.nickname+'”',p.occupation,p.ancestralVillage,p.branchGroup].filter(Boolean).slice(0,3).join(' · ')||conf(p.confidence)}</small></button>`).join('')}</div></section>`).join('')}</div></div>`;
  fb.querySelectorAll('.fcard').forEach(b=>b.onclick=()=>selectPerson(b.dataset.id,true));
  requestAnimationFrame(()=>fb.querySelector('.fcard.active')?.scrollIntoView({behavior:'smooth',block:'center',inline:'center'}));
}
function renderGraph(){
  document.getElementById('cy').style.display='none'; renderFallbackGraph(); return;
  if(typeof cytoscape==='undefined' || typeof dagre==='undefined') { document.getElementById('cy').style.display='none'; renderFallbackGraph(); return; }
  document.getElementById('cy').style.display='block';document.getElementById('fallback').classList.remove('show');if(cy)cy.destroy();
  try{cy=cytoscape({container:document.getElementById('cy'),elements:[...nodeData(),...edgeData()],wheelSensitivity:.22,minZoom:.22,maxZoom:2.4,style:[
{selector:'node',style:{'shape':'round-rectangle','width':210,'height':82,'background-color':'#111a29','border-width':1,'border-color':'rgba(255,255,255,.16)','label':'data(label)','color':'#f7f8fa','font-size':12,'font-weight':700,'text-wrap':'wrap','text-max-width':178,'text-valign':'center','text-halign':'center','overlay-opacity':0}},
{selector:'node[gender="Female"]',style:{'border-color':'#d5aa68'}},{selector:'node[confidence="tradition"]',style:{'border-style':'dashed','border-color':'#a991d7'}},{selector:'node:selected',style:{'border-width':2.5,'border-color':'#e8bc72','background-color':'#172338'}},
{selector:'edge[type="parent"]',style:{'width':2,'line-color':'#526176','target-arrow-shape':'triangle','target-arrow-color':'#526176','curve-style':'bezier','arrow-scale':.65,'opacity':.9}},
{selector:'edge[type="spouse"]',style:{'width':2,'line-style':'dashed','line-color':'#d9a95d','curve-style':'bezier','opacity':.8}}
],layout:{name:'dagre',rankDir:'TB',rankSep:100,nodeSep:55,edgeSep:15,animate:false,fit:true,padding:90}});
  cy.on('tap','node',e=>selectPerson(e.target.id(),true));setTimeout(()=>{const n=cy.getElementById(current);if(n.length){cy.$(':selected').unselect();n.select();cy.animate({center:{eles:n},zoom:Math.max(cy.zoom(),.85)},{duration:500})}},80)
  }catch(e){cy=null;document.getElementById('cy').style.display='none';renderFallbackGraph()}
}
function renderGenBar(){const g=groups(),cg=generation(byId(current));document.getElementById('genBar').innerHTML=g.map((arr,i)=>`<button class="genBtn ${i===cg?'active':''}" data-g="${i}">Generation ${i+1} · ${arr.length}</button>`).join('');document.querySelectorAll('.genBtn').forEach(b=>b.onclick=()=>jumpGen(+b.dataset.g))}
function jumpGen(g){const arr=groups()[g]||[];const pick=arr.find(p=>p.surname==='Bandiguptapu')||arr[0];if(pick)selectPerson(pick.id,true)}
function relCards(ids,label){const arr=[...new Set(ids)].map(byId).filter(Boolean);if(!arr.length)return`<div class="empty">No ${label.toLowerCase()} recorded</div>`;return `<div class="relList">${arr.map(p=>`<button class="rel" onclick="selectPerson('${p.id}',true)"><div class="mini">${initials(p)}</div><div><strong>${fullName(p)}</strong><span>${p.occupation||p.ancestralVillage||conf(p.confidence)}</span></div></button>`).join('')}</div>`}
function renderSide(){const p=byId(current);if(!p)return;const sib=new Set();p.parents.forEach(pid=>byId(pid)?.children.forEach(id=>{if(id!==p.id)sib.add(id)}));const d=(k,v)=>v?`<div class="detail"><label>${k}</label><div>${String(v).replaceAll('<','&lt;')}</div></div>`:'';document.getElementById('side').innerHTML=`<div class="profileHero"><div class="avatar">${initials(p)}</div><h1>${fullName(p)}</h1><div class="meta">${[p.nativeName,p.nickname&&'“'+p.nickname+'”',p.birthplace||p.ancestralVillage].filter(Boolean).join(' • ')}</div><span class="confidence">${conf(p.confidence)}</span><div class="quick"><button class="btn" onclick="openEdit('${p.id}')">Edit details</button><button class="btn primary" onclick="openRelation('child')">＋ Child</button><button class="btn" onclick="openRelation('parent')">＋ Parent</button><button class="btn" onclick="openRelation('spouse')">＋ Spouse</button></div></div>
<div class="section"><h3>Parents</h3>${relCards(p.parents,'Parents')}</div><div class="section"><h3>Spouse</h3>${relCards(p.spouses,'Spouses')}</div><div class="section"><h3>Children</h3>${relCards(p.children,'Children')}</div><div class="section"><h3>Siblings</h3>${relCards([...sib],'Siblings')}</div><div class="section"><h3>Details</h3><div class="detailGrid">${[d('Date of birth',p.dob||p.birthYear),d('Date of death',p.dod),d('Birthplace',p.birthplace),d('Ancestral village',p.ancestralVillage),d('Residence',p.residence),d('Occupation / role',p.occupation),d('Education',p.education),d('Gotram',p.gotram),d('Family deity',p.deity),d('Imported branch / group',p.branchGroup),d('Notes / family story',p.notes),d('Source / evidence',p.sourceNote)].join('')||'<div class="empty">No additional details yet</div>'}</div></div>`}
function updateSelectedMobile(){const p=byId(current),el=document.getElementById('selectedMobile');if(p&&el)el.innerHTML=`<b>${fullName(p)}</b><span>${[p.occupation,p.ancestralVillage,'Generation '+(generation(p)+1)].filter(Boolean).join(' • ')}</span>`}function selectPerson(id,animate){if(!byId(id))return;current=id;renderGenBar();renderSide();updateSelectedMobile();if(cy){cy.$(':selected').unselect();const n=cy.getElementById(id);n.select();if(animate)cy.animate({center:{eles:n},zoom:Math.max(cy.zoom(),.9)},{duration:620,easing:'ease-in-out-cubic'})}else{renderFallbackGraph()}if(window.gsap)gsap.fromTo('#side',{opacity:.45,x:12},{opacity:1,x:0,duration:.28,ease:'power2.out'})}
function render(){document.getElementById('personCount').textContent=people.length;document.getElementById('genCount').textContent=groups().length;renderGenBar();renderSide();updateSelectedMobile();renderGraph()}
// Search
const search=document.getElementById('search');search.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();if(!q){if(cy)cy.nodes().style('opacity',1);return}const hits=people.filter(p=>JSON.stringify(p).toLowerCase().includes(q)).map(p=>p.id);if(cy){cy.nodes().style('opacity',n=>hits.includes(n.id())?1: